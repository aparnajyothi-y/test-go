name: Generate Large Go Cache

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache: true

      - name: Create go.mod with large dependencies
        shell: pwsh
        run: |
          @"
          module bigcachetest
          go 1.22

          require (
            k8s.io/apimachinery v0.30.0
            k8s.io/client-go v0.30.0
            github.com/aws/aws-sdk-go v1.49.0
            github.com/google/go-containerregistry v0.18.0
            github.com/prometheus/client_golang v1.19.0
            github.com/hashicorp/consul v1.19.0
            google.golang.org/grpc v1.61.1
            go.opentelemetry.io/collector v0.105.0
            github.com/apache/arrow/go/v14 v14.0.2
          )
          "@ | Out-File -Encoding UTF8 go.mod

      - name: Create dummy main.go
        shell: pwsh
        run: |
          @"
          package main

          import (
            "fmt"
            _ "k8s.io/apimachinery/pkg/apis/meta/v1"
            _ "k8s.io/client-go/kubernetes"
            _ "github.com/aws/aws-sdk-go/aws"
            _ "github.com/google/go-containerregistry/pkg/v1"
            _ "github.com/prometheus/client_golang/prometheus"
            _ "github.com/hashicorp/consul/api"
            _ "google.golang.org/grpc"
            _ "go.opentelemetry.io/collector/component"
            _ "github.com/apache/arrow/go/v14/arrow"
          )

          func main() {
            fmt.Println("Building large Go module cache...")
          }
          "@ | Out-File -Encoding UTF8 main.go

      - name: Download and build modules
        shell: pwsh
        run: |
          Write-Host "Running go mod tidy..."
          go mod tidy -v

          Write-Host "Downloading modules..."
          go mod download -x

          Write-Host "Forcing go.sum generation..."
          go get ./...

          if (Test-Path go.sum) {
            Write-Host "✅ go.sum generated successfully."
          } else {
            Write-Error "❌ go.sum not found!"
          }

          Write-Host "Building project to populate build cache..."
          go build -v -o main.exe .

      - name: Show Go cache size
        shell: pwsh
        run: |
          $modCache = (go env GOMODCACHE)
          $buildCache = (go env GOCACHE)
          Write-Host "Module cache: $modCache"
          Write-Host "Build cache: $buildCache"

          if (Test-Path $modCache) {
            $sizeGB = (Get-ChildItem -Recurse $modCache -ErrorAction SilentlyContinue | Measure-Object -Property Length -Sum).Sum / 1GB
            Write-Host ("📦 Go module cache size: {0:N2} GB" -f $sizeGB)
          }

          if (Test-Path $buildCache) {
            $sizeGB2 = (Get-ChildItem -Recurse $buildCache -ErrorAction SilentlyContinue | Measure-Object -Property Length -Sum).Sum / 1GB
            Write-Host ("🛠️ Go build cache size: {0:N2} GB" -f $sizeGB2)
          }
